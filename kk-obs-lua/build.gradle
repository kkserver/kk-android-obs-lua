apply plugin: 'com.android.library'

android {
    compileSdkVersion 23
    buildToolsVersion "23.0.3"

    defaultConfig {
        minSdkVersion 10
        targetSdkVersion 23
        versionCode 1
        versionName "1.0"


    }
    buildTypes {
        release {
            minifyEnabled false
            proguardFiles getDefaultProguardFile('proguard-android.txt'), 'proguard-rules.pro'
        }
    }
}

dependencies {
    compile fileTree(include: ['*.jar'], dir: 'libs')
    compile 'kk:core:0.0.4@aar'
    compile 'kk:observer:0.0.5@aar'
    compile "kk:lua:0.0.3@aar"
    //compile project(':kk-lua')
    //compile project(':kk-obs')
    //compile project(':kk-core')
}

apply plugin: 'maven'

uploadArchives {
    repositories {
        mavenDeployer {
            pom.groupId = "kk"
            pom.artifactId = "observer-lua"
            pom.version = "0.0.1"
            repository(url: RELEASE_REPOSITORY_URL) {

            }
        }
    }
}


